import XCTest
@testable import JsonParserPackage

final class JsonParserPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(JsonParserPackage().text, "Hello, World!")
    }
}
